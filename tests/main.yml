---
- hosts: localhost
  connection: local
  vars:
    brew_taps:
      - homebrew/completions
    brew_packages:
      - wget
      - git
    cask_packages:
      - iterm2
  roles:
    - ansible-role-brew
