---
- hosts: localhost
  connection: local
  vars:
    brew_taps:
      - homebrew/completions
    brew_packages:
      - wget
      - git
    cask_packages:
      - dterm
  roles:
    - ansible-role-brew
